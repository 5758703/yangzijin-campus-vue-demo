# 验证记录

验证日期：2026-09-20。

## 自动检查

`npm test`：6 项通过。

1. OSM 署名、WGS84 经纬度范围、建筑编号唯一性。
2. 文津楼 relation/13817766 的两个内院孔洞保留，地标照片元数据存在。
3. 三个时刻含全部建筑、功率合计一致、文津楼告警及倒退/恢复清除、非法索引拒绝。
4. 三档人物数量、师生角色、东区跑道路线、循环路径连续采样。
5. 页面隐藏与减少动态效果暂停条件、楼宇祖先节点拾取、人物不被识别为楼宇。
6. 交付 GLB 为有效 glTF 二进制、保留全部91个 buildingId、几何范围为有限值。

## 浏览器操作验证

通过 Codex 内置 Chromium 浏览器实际操作：

- WebGL GLB 加载完成，校园可见；浏览器错误日志为空。
- 桌面默认全景、地标点击聚焦、俯视/三维/回到全景。
- 昭文馆、文津楼档案与照片随选择切换。
- 搜索“沁园”得到8栋宿舍；无匹配时显示提示；清空搜索恢复列表。
- 09:05 显示1条告警、文津楼32.4℃；09:10告警清除；回到起点重置。
- 人物密度调至高显示42人，关闭后状态消失，恢复中档显示24人。
- 地图来源弹窗可打开关闭，含515个要素、91栋轮廓、OSM署名与下载链接。
- 津园室内照片弹窗和笃行楼档案切换正常。
- 手机390×844、桌面1440×1100检查，页面宽度未超过视口。
- 预构建 dist 通过附带 Node 静态服务器在5192端口加载成功，GLB ready，参考照片加载成功，浏览器错误日志为空。
- 自动回放到09:10后停止，可回到09:00；人物位置随时间变化。

`npm run build` 成功。Three.js 分包约661 kB（gzip约167 kB），Vite 提示大于500 kB的体积警告，未影响构建。GLB约25.2 MB；没有做模型压缩，初次加载速度受设备与传输带宽影响。

ZIP 结构检查：包含源码、dist、原始 OSM、GeoJSON、GLB、5张参考图及文档；不含 node_modules 和 Python 缓存。可运行目录为 `yangzijin-twin`。

`preview-desktop.png` 和 `preview-mobile.png` 是本项目浏览器截图。人物动画逻辑和原有页面降级机制保留；未进行真实移动设备、所有浏览器、长时间负载测试。本次未运行 Blender 导入导出。

## 精度边界

本项目验证的是数据来源可追溯、模型可运行与交互行为，不是校区几何的测绘精度。高度和立面存在估算，校园内部地图要素可能不完整。运行指标和人物均为演示数据。
